interface:
  display_name: "Ad Image Localization"
  short_description: "Localize ads with imagegen-native sizes, QA, and flags"
  default_prompt: "Use $ad-image-localization with $imagegen to localize this image and generate the standard ad sizes natively."
