interface:
  display_name: "Ad Image Localization"
  short_description: "Localize, resize, QA, and flag ad-ready images"
  default_prompt: "Use $ad-image-localization with $imagegen to localize this image into platform-ready ad sizes."
